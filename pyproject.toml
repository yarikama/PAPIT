[project]
name = "papimg"
version = "0.1.0"
description = "Prompt-aware pruning baseline for image tokens using CLIP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"pillow>=10.0.0",
	"torch>=2.2.0",
	"transformers>=4.41.0",
]

[project.scripts]
papit = "main:main"
